<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.a1exs.recyclerviewexample">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecyclerViewExample">
        <activity
            android:name=".ui.AdvancedRecyclerViewActivity"
            android:exported="true" />
        <activity
            android:name=".ui.RecyclerViewActivity"
            android:exported="true" />
        <activity
            android:name=".ui.SmallListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.LargeListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>